services:
  - type: web
    name: yt-mod-monitor
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      # This rewrite rule ensures React handles all routing (if you add multiple pages later)
      - type: rewrite
        source: /*
        destination: /index.html
